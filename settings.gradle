include ':app'
rootProject.name = "Encyclopedia"

def compositeBuilds = [
        'framework/network',
        'framework/widget',
]

compositeBuilds.findAll { file(it).directory }.each { includeBuild(it) }